<!doctype html>

<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {% block titleAndDescription %}
      <title>{{ renderData.title or title }} | Eleventy Starter</title>
      <meta name="description" content="{{ description }}">
    {% endblock %}

    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#202020">
    {% include "partials/icons.njk" %}

    {% block vendorStyles %}{% endblock %}
    <link rel="stylesheet" href="/assets/css/styles.css">
    {% block pageStyles %}{% endblock %}
  </head>
  <body>
    <div class="cmp-spread">
      {% block header %}
        {% include "components/header.njk" %}
      {% endblock %}
      {% block content %}
        <main>
          <article class="cmp-container cmp-stack">
            <h1>
              {{ title }}
            </h1>
            <p class="util-size-sm util-font-italic">
              {{ page.date.toLocaleDateString() }}: Written by {{ author }}
            </p>
            {{ content | safe }}
          </article>
        </main>
      {% endblock %}
      {% block footer %}
        <footer class="cmp-container">
          <p>
            This is the footer of the website. It and the header are separated
            from the main content of the page (and from the top/bottom of the
            screen) by use of the spread component.
          </p>
        </footer>
      {% endblock %}
    </div>
    {% include "partials/dark-mode-favicon.njk" %}
    {% include "partials/update-auth-links.njk" %}
    {% block authScripts %}
      {# by default, do the simplest thing – pages with more auth stuff will replace this #}
      <script src="/assets/js/bundled/auth-state.js" type="module"></script>
      <script src="/assets/js/legacy/auth-state.js" nomodule></script>
    {% endblock %}
    {% block scripts %}{% endblock %}
    {% include "partials/service-worker.njk" %}
  </body>
</html>
